{
  "name": "@bite-track/api-client",
  "version": "0.0.1",
  "description": "Shared types across the monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist/**"],
  "author": "Daniel Diaz",
  "license": "MIT",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --minify",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Autogenerated package - No linting configured'"
  },
  "devDependencies": {
    "@bite-track/eslint-config": "*",
    "@bite-track/typescript-config": "*",
    "tsup": "^8.0.2"
  }
}
